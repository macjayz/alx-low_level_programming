#include "main.h"
/**
*_abs - This function computes the absolute value of an integer
*
*Return: Always 0 (Success)
*
*@ans: This variable receives a value from the main.c
*/
int _abs(int ans)
{
if (ans < 0)
ans *= -1;
else
ans;

return (ans);
}
